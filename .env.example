# Voice Intelligence App - Environment Variables
# ==============================================
#
# IMPORTANT: API keys should be entered via the Settings UI and are stored
# securely in your OS Keychain (not in .env files).
#
# This file is for local development reference only.
# NEVER commit actual API keys to version control.

# Optional: Override default settings for development
# DEFAULT_HOTKEY=CommandOrControl+Shift+Space
# DEFAULT_LANGUAGE=en
# RETENTION_DAYS=7

# LLM Provider API Keys (stored in OS Keychain via Settings UI)
# OPENAI_API_KEY=sk-...
# OPENROUTER_API_KEY=sk-or-...

# Development settings
# NODE_ENV=development
# TAURI_DEBUG=true
